# -*- coding: utf-8 -*-
"""第六章 寿命预测与健康监测"""
from build_report import add_heading_cn, add_body, add_page_break, add_table_simple, add_formula


def build_chapter6(doc):
    add_heading_cn(doc, "第六章 寿命预测与健康监测", level=1)

    add_body(doc, """
电容器健康监测（Capacitor Health Monitoring, CHM）与剩余使用寿命（Remaining Useful Life, RUL）预测是预测性维护（Predictive Maintenance）体系的核心技术。随着工业物联网与智能电网的快速发展，电力电子装备对无中断运行、按需维护的需求愈加迫切，CHM从离线检测向在线监测、从单参数监测向多参数融合、从统计判据向机器学习预测的演进已成为明确趋势。
""")

    add_heading_cn(doc, "6.1 电容器健康指标体系", level=2)
    add_body(doc, """
电容器健康指标（Health Indicator, HI）的选取应满足以下几个准则：
（1）可观测性——可在线测量或至少可近似推算；
（2）单调性——随老化呈现单调变化趋势；
（3）预测性——对失效时间有强相关；
（4）抗干扰性——对温度、工况波动不敏感。

综合国内外研究现状，主要健康指标包括：
（1）电容量 C：反映有效电极面积与介质介电常数；
（2）等效串联电阻 ESR：反映电极与电解液/介质的综合欧姆损耗；
（3）损耗因数 tanδ：反映介质极化损耗；
（4）漏电流 DCL/IR：反映介质绝缘性能；
（5）阻抗谱 Z(ω)：反映多尺度界面与介质响应；
（6）温升 ΔT：反映自发热，间接反映ESR与纹波；
（7）自愈脉冲频度 f_sh（针对MPFC）：反映内部缺陷积累；
（8）纹波电压/电流频谱：反映在线等效参数变化。

不同类型电容器的适宜HI组合不同：Al电解与超级电容以C、ESR联合为主；MLCC以IR、ΔC/C为主；金属化薄膜以C与f_sh为主；钽电容以DCL为主。
""")

    add_heading_cn(doc, "6.2 基于ESR与C的在线监测方法", level=2)
    add_heading_cn(doc, "6.2.1 直接测量法", level=3)
    add_body(doc, """
直接测量法通过在电容器端口施加已知激励、测量响应来辨识等效参数。典型方法包括：
（1）阻抗分析仪法：采用HP 4294A/Keysight E4990等精密阻抗分析仪在10 Hz~110 MHz范围内扫频测量Z(ω)，从中提取C与ESR；该方法精度高但需离线，仅适用于实验室；
（2）LCR表法：以100 Hz/1 kHz/10 kHz/100 kHz等标准频率测量C与Q值；
（3）谐振法：利用电容器自谐振频率fSR = 1/(2π√(LC))测量，适合小容量MLCC。
""")

    add_heading_cn(doc, "6.2.2 基于稳态纹波的在线辨识", level=3)
    add_body(doc, """
对于电力电子装备中的DC-link电容器，工作中存在开关频率产生的纹波电压与纹波电流。通过同步采样V_ripple(t)与I_ripple(t)，利用：
""")
    add_formula(doc, "C = ∫I_ripple·dt / ΔV")
    add_formula(doc, "ESR = (V_ripple_high - V_ripple_low) / ΔI")
    add_body(doc, """
可在不注入额外激励的情况下完成在线辨识。Venet等（2002、2008）系统发展了此类方法并应用于变频器在线监测[47]。Abdennadher等（2012）进一步提出了基于开关周期同步采样与最小二乘辨识的算法，实测精度可达C:±3%、ESR:±10%以内[48]。这类方法的优势是无侵入、成本低；局限是对纹波信号信噪比要求较高，在轻载条件下精度下降。
""")

    add_heading_cn(doc, "6.2.3 基于注入扰动的辨识", level=3)
    add_body(doc, """
在某些对监测精度要求极高的场合，可通过有意注入低幅度的扰动信号（小正弦扰动、伪随机序列PRBS、阶跃等）激发电容器响应，通过系统辨识算法（RLS、Kalman滤波等）提取参数。Sankaran等（2010）采用1 kHz小正弦扰动的方法对Al-cap实现了C±1.5%、ESR±5%的在线辨识精度[27]。
""")

    add_heading_cn(doc, "6.3 阻抗谱与频域监测方法", level=2)
    add_body(doc, """
电化学阻抗谱（Electrochemical Impedance Spectroscopy, EIS）是超级电容与铝电解电容的标准表征方法。EIS可将电容器在频域上的响应分解为多个尺度的物理过程：
（1）高频段（>10 kHz）：电极与引线电感、端电极接触电阻；
（2）中频段（1 Hz~10 kHz）：电解液/介质欧姆阻抗、电极孔隙扩散；
（3）低频段（<1 Hz）：电极/电解液界面电荷转移、SEI膜、扩散尾。

通过等效电路模型（如Randles电路、De Levie多孔电极模型）拟合EIS可以获得深入的机理信息。Kurzweil等（2015）通过EIS跟踪超级电容全寿命周期中不同频段阻抗的演化，发现高频ESR上升与低频扩散阻抗上升源于不同机理，需要分别分析[42]。Nature Communications（2022）发表的基于动态EIS（dynamic EIS）的电池/电容器诊断方法进一步提高了时间分辨率，可捕捉分钟尺度的健康指标演化。

对于现场工程应用，可采用"关键频点阻抗法"——仅选取2~5个敏感频点进行在线测量，既兼顾信息量又控制测量成本。这一方法在风电变流器、UPS系统中已有成熟部署。
""")

    add_heading_cn(doc, "6.4 基于机器学习的RUL预测", level=2)
    add_heading_cn(doc, "6.4.1 数据准备与特征工程", level=3)
    add_body(doc, """
基于机器学习的RUL预测一般需要构建如下数据基础：
（1）训练集来源：加速老化试验中从初始时刻到失效时刻的全过程监测数据，以及实际现场的类似数据；
（2）输入特征：C、ESR、温度、电压、电流及其统计特征（均值、方差、趋势、频谱）；
（3）输出标签：剩余使用寿命（RUL）或直接失效时间（TTF）；
（4）数据预处理：归一化、SG滤波降噪、VMD分解、缺失值插补、窗口分割。

Yan等（2020）采用SG-VMD-LSTM复合方法对超级电容RUL预测进行了系统研究，结果显示相较传统LSTM可降低RMSE约30%[49]；Zhu等（2025）进一步在CNN-LSTM框架下引入参数离散度作为显式输入，在考虑器件个体差异的情况下提升预测鲁棒性[15]。
""")

    add_heading_cn(doc, "6.4.2 LSTM与CNN-LSTM模型", level=3)
    add_body(doc, """
LSTM通过门控机制处理长序列依赖，特别适合电容器这种长时老化过程的建模。典型CNN-LSTM架构如下：
（1）一维CNN层提取局部时序特征（捕捉纹波细节、自愈脉冲等）；
（2）LSTM层建模长时退化趋势（C与ESR的累积演化）；
（3）全连接层输出RUL估计。

Soualhi等（2022）建立了考虑动态工况的Al-cap LSTM模型，能够处理现场监测中常见的工况突变问题，相较固定工况下的LSTM模型，RMSE降低了约40%[45]。MDPI Electronics（2025）提出的考虑参数离散的CNN-LSTM模型，利用集成多个电容器的辨识结果作为模型输入，进一步提升了预测鲁棒性[15]。
""")

    add_heading_cn(doc, "6.4.3 物理信息神经网络（PINN）", level=3)
    add_body(doc, """
物理信息神经网络将物理方程作为损失函数的约束项，具有以下形式：
""")
    add_formula(doc, "L_total = L_data + λ·L_physics")
    add_body(doc, """
其中L_data是传统监督损失（如MSE），L_physics是物理方程残差（如Arrhenius微分方程、SEI膜增厚方程）。PINN的优势在于：即使训练数据稀少，由于物理方程的"正则化"作用，模型仍能给出合理外推。

近期（2025）报道的基于LSTM骨架的超级电容PINN模型，在仅使用20%的训练数据条件下仍可达到与传统方法使用100%数据相当的预测精度[6]。AIP APL Machine Learning（Kim等，2023）提出的物理约束机器学习方法用于MLCC失效时间预测，将Prokopowicz-Vaskas方程作为先验约束，在不同电压应力之间的外推精度显著提升[14]。

物理-数据混合模型代表了当前电容器RUL预测的最新方向，预计在下一个五年将成为主流方法。
""")

    add_heading_cn(doc, "6.5 监测系统架构", level=2)
    add_body(doc, """
典型的电容器在线健康监测系统架构可分为五层：
（1）感知层：电压/电流传感器、温度传感器、振动传感器；
（2）采集层：高速ADC、同步采样、FPGA预处理；
（3）辨识层：在线C/ESR辨识、特征提取；
（4）诊断层：健康状态评估、失效判据触发；
（5）预测层：RUL估计、维护决策。

在工程部署中，需要综合考虑实时性（辨识周期从分钟级到小时级）、数据带宽（MB/小时至GB/天）、计算资源（嵌入式FPGA/DSP到云端服务器）与诊断延迟（秒级预警到月级预测）等多方面需求。对于风电、光伏这类分布式发电场景，采用"边缘-云"协同架构正在成为主流：边缘侧执行实时辨识与特征提取，云端进行大数据训练与全生命周期管理。
""")
    add_page_break(doc)
